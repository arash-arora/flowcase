### Flowcase environment example
# Copy this file to ".env" and fill in secrets/host values.
# Keep the real .env out of source control.

# Toggle Flask debug mode (1 to enable)
FLASK_DEBUG=0

# Authentik / OIDC configuration
# Preferred: point to the full metadata URL for your authentik application
# Example metadata URL: https://auth.zims.in:13254/application/o/flowcase1/.well-known/openid-configuration
AUTHENTIK_METADATA_URL=

# Alternatively, set issuer + explicit endpoint URLs (if you don't use metadata)
# Exampl e issuer: https://auth.zims.in:13254/application/o/flowcase1/
AUTHENTIK_ISSUER=
AUTHENTIK_CLIENT_ID=
AUTHENTIK_CLIENT_SECRET=
AUTHENTIK_REDIRECT_URI=

# Optional: explicit endpoint fallbacks (only needed if discovery is unavailable)
AUTHENTIK_AUTHORIZATION_URL=
AUTHENTIK_TOKEN_URL=
AUTHENTIK_USERINFO_URL=

# Optional: keep a server-side secret key here instead of the file-based secret (not required)
# If you set SECRET_KEY, it will override the value read from data/secret_key
SECRET_KEY=

# Notes:
# - For local testing behind HTTP, set AUTHENTIK_REDIRECT_URI accordingly and register
#   the exact redirect URI in Authentik. For production, use HTTPS.
# - Do NOT commit your real client secret to source control. Use a secret manager where practical.
